# Ensure Spring @Lazy on fields is copied onto Lombok-generated constructor parameters.
# Without this, circular dependencies fail at startup even when fields are marked @Lazy.
lombok.copyableAnnotations += org.springframework.context.annotation.Lazy
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
