package com.gitutility.model.enums;

public enum SyncStatus {
    QUEUED,
    IN_PROGRESS,
    SUCCESS,
    FAILED,
    SKIPPED,
    DEAD_LETTERED,
    CONFLICT_ISOLATED,
    CANCELLED,
    INTERRUPTED,
    PAUSED
}
