{
  "name": "gitmirror-webhook-worker-kafka",
  "version": "1.0.0",
  "description": "Cloudflare Worker that publishes normalized git events to Kafka. Separate from the RabbitMQ webhook-worker.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "node scripts/use-env.mjs dev",
    "deploy": "node scripts/use-env.mjs deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260829.1",
    "typescript": "^5.5.4",
    "wrangler": "^4.127.1"
  }
}
